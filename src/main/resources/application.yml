spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jayz_dogs
    username: postgres
    password: ada
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: none
